'use strict';

const http=require('http');
const crypto=require('crypto');
const fs=require('fs');
const path=require('path');
const {spawn}=require('child_process');
const {Pool}=require('pg');
const {WebSocketServer,WebSocket}=require('ws');

const PORT=Number(process.env.PORT||8080),HOST=process.env.HOST||'0.0.0.0',GAME_PORT=Number(process.env.GAME_PORT||PORT+1),DATABASE_URL=process.env.DATABASE_URL||'',MAX_MSG=16*1024,BACKEND_URL=`ws://127.0.0.1:${GAME_PORT}`;
const GM_ACCOUNTS=new Set(String(process.env.GM_ACCOUNTS||'').split(',').map(x=>x.trim().toLowerCase()).filter(Boolean));
if(!DATABASE_URL){console.error('DATABASE_URL is required for account authentication.');process.exit(1)}
const db=new Pool({connectionString:DATABASE_URL,ssl:process.env.NODE_ENV==='production'?{rejectUnauthorized:false}:undefined});
const cleanAccount=v=>String(v||'').trim().toLowerCase().slice(0,32),validAccount=a=>/^[a-z0-9_.-]{4,32}$/.test(a),validPassword=p=>typeof p==='string'&&p.length>=4&&p.length<=72;
const CLASS_RANGES={royal:{str:[11,16],dex:[9,15],con:[10,16],int:[8,13],wis:[9,15],cha:[12,18]},knight:{str:[14,18],dex:[10,16],con:[14,18],int:[7,11],wis:[8,13],cha:[8,12]},mage:{str:[8,13],dex:[8,14],con:[8,14],int:[14,18],wis:[14,18],cha:[8,14]},elf:{str:[10,16],dex:[14,18],con:[10,16],int:[10,16],wis:[11,17],cha:[9,15]}};
const rint=(a,b)=>a+Math.floor(Math.random()*(b-a+1));
function rollClassStats(cls){const r=CLASS_RANGES[cls];if(!r)return null;const out={};for(const k of ['str','dex','con','int','wis','cha'])out[k]=rint(r[k][0],r[k][1]);return out}
function hashPassword(p,s){return crypto.scryptSync(p,Buffer.from(s,'hex'),64).toString('hex')}
function makePassword(p){const salt=crypto.randomBytes(16).toString('hex');return{salt,hash:hashPassword(p,salt)}}
function safeEqualHex(a,b){try{const x=Buffer.from(a,'hex'),y=Buffer.from(b,'hex');return x.length===y.length&&crypto.timingSafeEqual(x,y)}catch(_){return false}}
function send(ws,type,payload){if(ws&&ws.readyState===WebSocket.OPEN)ws.send(JSON.stringify({type,payload:payload||{}}))}
function fail(ws,code,message){send(ws,'error',{code,message})}
function isGmAccount(account){return GM_ACCOUNTS.has(cleanAccount(account))}
async function initDb(){await db.query(`CREATE TABLE IF NOT EXISTS accounts(username VARCHAR(32) PRIMARY KEY,password_salt VARCHAR(64) NOT NULL,password_hash VARCHAR(256) NOT NULL,character_token VARCHAR(96) UNIQUE NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),last_login_at TIMESTAMPTZ)`);await db.query('CREATE INDEX IF NOT EXISTS idx_accounts_character_token ON accounts(character_token)');console.log(`PostgreSQL account authentication ready; GM accounts configured=${GM_ACCOUNTS.size}`)}

function buildRuntimeServer(){
 const sourcePath=path.join(__dirname,'server.js'),runtimePath=path.join(__dirname,'.server-runtime.js');let src=fs.readFileSync(sourcePath,'utf8');
 const patch=(a,b,label)=>{if(src.includes(a)){src=src.replace(a,b);console.log('[runtime patch]',label);return true}console.warn('[runtime patch skipped]',label);return false};
 patch("function newCharacter(name,cls){const b=baseForClass(cls),stats=rollStats(cls);return","function newCharacter(name,cls,rolledStats){const b=baseForClass(cls),stats=(rolledStats&&typeof rolledStats==='object')?rolledStats:rollStats(cls);return",'server rolled stats');
 patch('char=newCharacter(name,cls);','char=newCharacter(name,cls,p.stats);','use gateway rolled stats');
 patch('mob.respawnAt=Date.now()+5000;','mob.respawnAt=Date.now()+rint(1000,3000);','monster respawn 1-3 sec');
 patch('dead:m.dead}))};','dead:m.dead,respawnAt:m.respawnAt}))};','expose respawnAt');
 patch('if(Math.random()<chance)out.push({id,cnt});','if(Math.random()<Math.min(0.85,chance*2.5))out.push({id,cnt});','drop multiplier');
 patch('client.gm=await isGmToken(token);','client.gm=(p.trustedGm===true)||await isGmToken(token);','trusted GM propagation');
 patch("function handleBuy(client,p){const c=clientChar(client);if(!c)return;if(!MAPS[c.map]||!MAPS[c.map].safe)return error(client.ws,'只有安全區可以購物。');const id=cleanText(p.id,80),d=ITEM_CATALOG[id];","function handleBuy(client,p){const c=clientChar(client);if(!c)return;const id=cleanText(p.id,80),d=ITEM_CATALOG[id];",'allow shop purchases on all maps');
 const marker='async function handleLogin(client,p)',extra=`
const MARKET_FILE=path.join(__dirname,'data','market.json');let marketListings=[];try{marketListings=JSON.parse(fs.readFileSync(MARKET_FILE,'utf8'));if(!Array.isArray(marketListings))marketListings=[];}catch(_){marketListings=[];}function saveMarket(){try{fs.mkdirSync(path.dirname(MARKET_FILE),{recursive:true});fs.writeFileSync(MARKET_FILE,JSON.stringify(marketListings,null,2));}catch(e){console.error('market save failed',e&&e.message);}}function publicMarket(viewerToken){return marketListings.map(x=>({id:x.id,itemId:x.itemId,itemName:(ITEM_CATALOG[x.itemId]||{}).name||x.itemId,qty:x.qty,price:x.price,en:x.en||0,bless:!!x.bless,sellerName:x.sellerName,createdAt:x.createdAt,mine:x.sellerToken===viewerToken}));}function sendMarket(client){send(client.ws,'market_snapshot',{listings:publicMarket(client.token)});}function broadcastMarket(){for(const cl of clients.values())if(cl.token)sendMarket(cl);}function handleMarket(client,p){const c=clientChar(client);if(!c)return;const action=cleanText(p.action,20);if(action==='list'){sendMarket(client);return;}if(action==='sell'){const it=findInv(c,cleanText(p.uid,40));if(!it)return error(client.ws,'找不到要上架的物品。');if(itemEquipped(c,it.uid))return error(client.ws,'裝備中的物品不能上架。');if(it.lock)return error(client.ws,'鎖定中的物品不能上架。');const qty=Math.max(1,Math.min(Number(it.cnt)||1,Math.floor(Number(p.qty)||1))),price=Math.max(1,Math.min(999999999,Math.floor(Number(p.price)||0)));if(marketListings.filter(x=>x.sellerToken===client.token).length>=20)return error(client.ws,'最多只能同時上架 20 筆。');const listing={id:uid('mk'),sellerToken:client.token,sellerName:c.name,itemId:it.id,qty,price,en:Number(it.en)||0,bless:!!it.bless,createdAt:Date.now()};removeItem(c,it.uid,qty);marketListings.push(listing);saveMarket();commit(client,{type:'market_sell',listingId:listing.id,itemId:listing.itemId,qty,price});broadcastMarket();return;}if(action==='cancel'){const id=cleanText(p.id,60),idx=marketListings.findIndex(x=>x.id===id&&x.sellerToken===client.token);if(idx<0)return error(client.ws,'找不到你的上架商品。');const x=marketListings[idx];addItem(c,x.itemId,x.qty,{en:x.en,bless:x.bless});marketListings.splice(idx,1);saveMarket();commit(client,{type:'market_cancel',listingId:id,itemId:x.itemId,qty:x.qty});broadcastMarket();return;}if(action==='buy'){const id=cleanText(p.id,60),idx=marketListings.findIndex(x=>x.id===id);if(idx<0)return error(client.ws,'商品已被購買或下架。');const x=marketListings[idx];if(x.sellerToken===client.token)return error(client.ws,'不能購買自己上架的商品。');if(c.gold<x.price)return error(client.ws,'金幣不足。');const seller=characters.get(x.sellerToken);if(!seller)return error(client.ws,'賣家資料暫時無法讀取。');c.gold-=x.price;seller.gold=(Number(seller.gold)||0)+x.price;addItem(c,x.itemId,x.qty,{en:x.en,bless:x.bless});marketListings.splice(idx,1);markDirty(client.token);markDirty(x.sellerToken);saveMarket();commit(client,{type:'market_buy',listingId:id,itemId:x.itemId,qty:x.qty,price:x.price,seller:x.sellerName});const sc=[...clients.values()].find(cl=>cl.token===x.sellerToken);if(sc)sendSnapshot(sc,{type:'market_sold',listingId:id,itemId:x.itemId,qty:x.qty,price:x.price,buyer:c.name});broadcastMarket();return;}error(client.ws,'未知的交易所操作。');}function ensureAfkFlags(c){c.flags=c.flags&&typeof c.flags==='object'?c.flags:{};if(typeof c.flags.offlineAfkEnabled!=='boolean')c.flags.offlineAfkEnabled=false;return c.flags;}function handleAfk(client,p){const c=clientChar(client);if(!c)return;const flags=ensureAfkFlags(c);flags.offlineAfkEnabled=!!p.enabled;flags.offlineAfkSince=flags.offlineAfkEnabled?Date.now():0;markDirty(client.token);commit(client,{type:'afk_toggle',enabled:flags.offlineAfkEnabled});send(client.ws,'afk_status',{enabled:flags.offlineAfkEnabled,maxHours:8});}function settleOfflineAfk(c,token){const flags=ensureAfkFlags(c),since=Number(flags.offlineAfkSince)||0;flags.offlineAfkSince=0;if(!flags.offlineAfkEnabled||!since)return null;const seconds=Math.max(0,Math.min(8*3600,Math.floor((Date.now()-since)/1000))),map=MAPS[c.map];if(!map||map.safe||!Array.isArray(map.pool)||!map.pool.length)return {type:'offline_afk',seconds,kills:0,exp:0,gold:0,drops:[]};if(seconds<5)return {type:'offline_afk',seconds,kills:0,exp:0,gold:0,drops:[]};recalc(c);const interval=Math.max(6,14-Math.min(6,Math.floor((Number(c.lv)||1)/10))),kills=Math.min(4000,Math.floor(seconds/interval));let exp=0,gold=0;const totals=new Map();for(let i=0;i<kills;i++){const id=map.pool[rint(0,map.pool.length-1)],st=MOB_STATS[id]||[1,20,3,1],lv=Number(st[0])||1;exp+=Math.max(5,lv*7);gold+=rint(Math.max(1,lv*2),Math.max(3,lv*5));for(const d of rollDrops({templateId:id}))totals.set(d.id,(totals.get(d.id)||0)+d.cnt);}c.exp+=exp;c.gold+=gold;for(const [id,cnt]of totals)addItem(c,id,cnt);checkLevel(c);c.hp=c.maxHp;c.mp=c.maxMp;markDirty(token);return {type:'offline_afk',seconds,kills,exp,gold,drops:[...totals].map(([id,cnt])=>({id,cnt}))};}
`;
 if(!src.includes('function handleMarket(client,p)')){if(!src.includes(marker))throw new Error('server.js is missing handleLogin; cannot build runtime safely');src=src.replace(marker,extra+'\n'+marker);console.log('[runtime patch] market and offline AFK helpers');}
 patch("send(client.ws,'auth_ok',{token,character:publicChar(char,client.id)});sendSnapshot(client,{type:'login'});presence();send(client.ws,'gm_status',{gm:!!client.gm,menu:client.gm?POS_MENU:[],boss:publicBoss()});","const afkResult=settleOfflineAfk(char,token);send(client.ws,'auth_ok',{token,character:publicChar(char,client.id)});sendSnapshot(client,afkResult||{type:'login'});send(client.ws,'afk_status',{enabled:!!char.flags?.offlineAfkEnabled,maxHours:8});presence();send(client.ws,'gm_status',{gm:!!client.gm,menu:client.gm?POS_MENU:[],boss:publicBoss()});",'offline AFK login settlement');
 patch("ws.on('close',()=>{const c=clientChar(client),map=c&&c.map;clients.delete(id);if(map)broadcastMap(map,{type:'player_disconnect',id});presence();});","ws.on('close',()=>{const c=clientChar(client),map=c&&c.map;if(c&&c.flags?.offlineAfkEnabled){c.flags.offlineAfkSince=Date.now();markDirty(client.token);flushDirty().catch(()=>{});}clients.delete(id);if(map)broadcastMap(map,{type:'player_disconnect',id});presence();});",'offline AFK disconnect timestamp');
 if(!src.includes("msg.type==='market'"))patch("if(msg.type==='warehouse')return handleWarehouse(client,p);","if(msg.type==='warehouse')return handleWarehouse(client,p);if(msg.type==='market')return handleMarket(client,p);if(msg.type==='afk')return handleAfk(client,p);",'market/afk dispatch');else if(!src.includes("msg.type==='afk'"))patch("if(msg.type==='market')return handleMarket(client,p);","if(msg.type==='market')return handleMarket(client,p);if(msg.type==='afk')return handleAfk(client,p);",'afk dispatch');
 fs.writeFileSync(runtimePath,src);return runtimePath;
}
let runtimeServer;try{runtimeServer=buildRuntimeServer()}catch(e){console.error('Cannot prepare game runtime:',e);process.exit(1)}
const child=spawn(process.execPath,[runtimeServer],{cwd:__dirname,env:Object.assign({},process.env,{PORT:String(GAME_PORT),HOST:'127.0.0.1'}),stdio:'inherit'});child.on('exit',(code,signal)=>{console.error(`Game server exited code=${code} signal=${signal||''}`);process.exit(code||1)});
function openBackend(onOpen,onMessage,onClose,onError){const b=new WebSocket(BACKEND_URL,{maxPayload:MAX_MSG});b.on('open',()=>onOpen(b));b.on('message',d=>onMessage(b,d));b.on('close',()=>onClose&&onClose());b.on('error',e=>onError&&onError(e));return b}
async function authenticateAccount(account,password){const r=await db.query('SELECT username,password_salt,password_hash,character_token FROM accounts WHERE username=$1 LIMIT 1',[account]);if(!r.rows.length)return{error:'account_not_found'};const row=r.rows[0],candidate=hashPassword(password,row.password_salt);if(!safeEqualHex(candidate,row.password_hash))return{error:'wrong_password'};return{row}}
async function accountForToken(token){if(!token)return'';try{const r=await db.query('SELECT username FROM accounts WHERE character_token=$1 LIMIT 1',[token]);return cleanAccount(r.rows[0]?.username||'')}catch(_){return''}}

const activeSessions=new Map();
function claimSession(key,client,replaceExisting=false){
 key=String(key||'');if(!key)return false;
 const old=activeSessions.get(key);
 if(old&&old!==client&&old.readyState===WebSocket.OPEN){
  if(!replaceExisting)return false;
  send(old,'session_kicked',{code:'session_replaced',message:'有人登入您的帳號，帳號可能被盜用。'});
  setTimeout(()=>{try{old.close(4001,'session_replaced')}catch(_){}},150);
 }
 activeSessions.set(key,client);client.sessionKey=key;return true;
}
function releaseSession(client){if(client.sessionKey&&activeSessions.get(client.sessionKey)===client)activeSessions.delete(client.sessionKey);client.sessionKey=''}

const server=http.createServer(async(req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Content-Type','application/json; charset=utf-8');if(req.url==='/health'){try{const c=await db.query('SELECT COUNT(*)::int AS n FROM accounts');res.end(JSON.stringify({ok:true,world:'1.49',authority:'server',persistence:'postgresql',authentication:'postgresql-scrypt',singleSession:true,accounts:c.rows[0].n,gmAccountsConfigured:GM_ACCOUNTS.size}))}catch(_){res.statusCode=500;res.end(JSON.stringify({ok:false,error:'database_unavailable'}))}return}res.end(JSON.stringify({name:'MMO2 1.49 auth gateway',authentication:'postgresql-scrypt',singleSession:true,gmAccountsConfigured:GM_ACCOUNTS.size}))});
const wss=new WebSocketServer({server,maxPayload:MAX_MSG});
wss.on('connection',client=>{let backend=null,authenticated=false,authInProgress=false;const rolls=new Map();send(client,'welcome',{world:'1.49',authority:'server',authentication:'postgresql'});
 function attach(token,registration,sessionKey,gm,replaceExisting=false){if(backend)try{backend.close()}catch(_){}let ready=false,gmMenu=[];backend=openBackend(ws=>{ready=true;const payload=registration?{token:'',name:registration.name,cls:registration.cls,stats:registration.stats,trustedGm:!!gm}:{token,trustedGm:!!gm};ws.send(JSON.stringify({type:'auth',payload}))},async(ws,data)=>{let packet;try{packet=JSON.parse(data.toString())}catch(_){return}if(packet.type==='welcome')return;if(packet.type==='gm_status'){gmMenu=Array.isArray(packet.payload?.menu)?packet.payload.menu:[];send(client,'gm_status',{gm:!!gm,menu:gm?gmMenu:[],boss:packet.payload?.boss||null});return}if(registration&&packet.type==='auth_ok'){const newToken=packet.payload&&packet.payload.token;if(!newToken){authInProgress=false;return fail(client,'registration_failed','建立角色失敗。')}const creds=makePassword(registration.password);try{await db.query('INSERT INTO accounts(username,password_salt,password_hash,character_token,last_login_at) VALUES($1,$2,$3,$4,NOW())',[registration.account,creds.salt,creds.hash,newToken])}catch(e){authInProgress=false;try{ws.close()}catch(_){};return fail(client,e&&e.code==='23505'?'account_exists':'registration_failed',e&&e.code==='23505'?'這個帳號已經有人使用。':'帳號建立失敗，請稍後再試。')}authenticated=true;authInProgress=false;claimSession(registration.account,client,false);packet.payload.account=registration.account;packet.payload.auth='postgresql';packet.payload.gm=!!gm;packet.payload.gmMenu=gm?gmMenu:[];send(client,packet.type,packet.payload);return}if(!registration&&packet.type==='auth_ok'){authenticated=true;authInProgress=false;claimSession(sessionKey||token,client,replaceExisting);packet.payload.auth='postgresql';packet.payload.gm=!!gm;packet.payload.gmMenu=gm?gmMenu:[];send(client,packet.type,packet.payload);return}if(packet.type==='error'&&!authenticated)authInProgress=false;if(client.readyState===WebSocket.OPEN)client.send(data.toString())},()=>{if(authenticated&&client.readyState===WebSocket.OPEN){fail(client,'game_backend_closed','遊戲伺服器連線中斷。');try{client.close()}catch(_){}}},()=>{if(!ready){authInProgress=false;fail(client,'game_backend_unavailable','遊戲伺服器啟動中，請稍後再試。')}})}
 client.on('message',async raw=>{let msg;try{msg=JSON.parse(raw.toString())}catch(_){return}const p=msg.payload||{};if(msg.type==='ping'&&!authenticated)return send(client,'pong',{t:Date.now()});if(msg.type==='roll_stats'&&!authenticated){const cls=String(p.cls||''),stats=rollClassStats(cls);if(!stats)return fail(client,'invalid_class','請先選擇職業。');const rollId=crypto.randomBytes(16).toString('hex');rolls.set(rollId,{cls,stats,expires:Date.now()+10*60*1000});return send(client,'roll_stats',{rollId,cls,stats})}if(msg.type==='auth'){if(authInProgress)return;authInProgress=true;const legacyToken=String(p.token||'').trim().slice(0,96),account=cleanAccount(p.account),password=String(p.password||'');if(p.register===true){if(!validAccount(account)||!validPassword(password)){authInProgress=false;return fail(client,'invalid_credentials','帳號或密碼格式不正確。')}const name=String(p.name||'').trim().slice(0,12),cls=String(p.cls||''),rollId=String(p.rollId||''),roll=rolls.get(rollId);if(name.length<2||!CLASS_RANGES[cls]){authInProgress=false;return fail(client,'invalid_character','請輸入 2～12 字角色名稱並選擇職業。')}if(!roll||roll.cls!==cls||roll.expires<Date.now()){authInProgress=false;return fail(client,'invalid_roll','請重新按「Server 擲骰」取得角色能力值。')}rolls.delete(rollId);const exists=await db.query('SELECT 1 FROM accounts WHERE username=$1 LIMIT 1',[account]);if(exists.rows.length){authInProgress=false;return fail(client,'account_exists','這個帳號已經有人使用。')}return attach('',{account,password,name,cls,stats:roll.stats},account,isGmAccount(account),false)}if(account||password){if(!validAccount(account)||!validPassword(password)){authInProgress=false;return fail(client,'invalid_credentials','帳號或密碼格式不正確。')}try{const r=await authenticateAccount(account,password);if(r.error){authInProgress=false;return fail(client,r.error,r.error==='account_not_found'?'找不到這個帳號。':'帳號或密碼錯誤。')}await db.query('UPDATE accounts SET last_login_at=NOW() WHERE username=$1',[account]);return attach(r.row.character_token,null,account,isGmAccount(account),true)}catch(_){authInProgress=false;return fail(client,'database_error','資料庫驗證失敗，請稍後再試。')}}if(legacyToken){const legacyAccount=await accountForToken(legacyToken);return attach(legacyToken,null,legacyAccount||legacyToken,isGmAccount(legacyAccount),false)}authInProgress=false;return fail(client,'invalid_credentials','請輸入帳號與密碼。')}if(!authenticated||!backend||backend.readyState!==WebSocket.OPEN)return fail(client,'not_authenticated','請先登入帳號。');backend.send(raw.toString())});
 client.on('close',()=>{releaseSession(client);if(backend)try{backend.close()}catch(_){}})
});
(async()=>{try{await initDb();server.listen(PORT,HOST,()=>console.log(`MMO2 auth gateway on ${HOST}:${PORT}; game backend ${BACKEND_URL}; GM configured=${GM_ACCOUNTS.size}`))}catch(e){console.error('Auth gateway startup failed:',e);try{child.kill()}catch(_){}process.exit(1)}})();
async function shutdown(){try{child.kill('SIGTERM')}catch(_){}try{fs.unlinkSync(runtimeServer)}catch(_){}try{await db.end()}catch(_){}process.exit(0)}
process.on('SIGINT',shutdown);process.on('SIGTERM',shutdown);
